// import apiInstance from "./apiInstance";

// const fetcher = async (key, params) => {
//   const resp = await apiInstance.get(`/route?id=${key}&param=${params}`).then(res => res.data);
//   return resp;
// };
// const { data, error } = useSWR([key, params], fetcher);

// const api = {
//   get: async (url) => {
//     const response = await axios.get(`${baseURL}${url}`);
//     return response.data;
//   },

//   post: async (url, body) => {
//     const response = await axios.post(`${baseURL}${url}`, body);
//     return response.data;
//   },

//   // Add other HTTP methods (put, patch, delete, etc.) if needed
// };

